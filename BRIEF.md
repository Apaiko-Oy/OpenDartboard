# Brief: #1560 — is the wire residual one lens constant? k1 (and f) per camera, from footage

You are carrying **turnaus#1560** (`gh --repo Apaiko-Oy/turnaus`). Worktree: `C:\Projektit\od-wt-1560`, branch `issue-1560-k1-census`, cut from main `4e988d2`. Read the full issue body AND its comments first — the maintainer's OV9732 inventory comment extends the scope: report **f (focal length in px) alongside k1 per camera**. Then read the simulation comment on closed issue turnaus#1513 (2026-09-23): it is the model this census implements, including the signal-size tables and the trap. Commit early and often.

## The question, exactly

Per camera, over `mocks/rig-20260918` and `mocks/rig-20260922`: is #1467's wire residual (rms 1.63°) **systematic** — one Brown–Conrady k1 explaining bow-vs-radius and the doubles-ring conic residual — or random? Fit one k1 (and f) per camera; report residual before/after. Systematic → the first measured intrinsics this repository has, recorded where constants live with the census beside them. Random → say so with numbers; that prices #1513's (closed) target-capture question. The verdict goes to both issues either way.

## Hard facts you inherit (docs/rig.md, do not re-derive)

- Sensor: OV9732, native 1280x720, 3.0 um square pixels, no crop — f_px = f_mm/0.003, square-pixel assumption datasheet-backed.
- Lens: wide variant, f expected ~400–600 px (72°/3.6 mm excluded by arithmetic); "no deformity" marketing predicts small k1. The hard-coded 120°/424 px in `perspective_processing.cpp` is the guess your f measurement checks.
- **The trap (binding):** radial distortion cannot bend a line through the distortion centre. Report per camera where the board sits in the frame and the sensitivity that position buys; a centred board answers "cannot tell from this footage", never "k1 = 0".
- Registry (dev) builds calibrate at a 3 s seek window (#1551); `OD_SEEK_VIDEO=off` reaches the opening. For static wires either window works, but SAY which one your frames come from, and note rig-22 cam1 only fits at the opening window.

## Where the data is

- The fitted wire/ring machinery: `wire_processing.*`, `ellipse_processing.*` (its constants cite `i1499_band_census.cpp`'s read — that instrument, marked unrun-tester, is the house pattern for yours), `board_model.hpp`'s fit for the per-camera geometry.
- Calibration-window frames: extract via the detector's own averaged frame or `i1511_frame_tool.cpp`'s frame-dump idiom. Wire candidate endpoints live in the calibration transcripts; prior whole-clip logs with full calibration transcripts: `C:\Projektit\runs-od-wt-1551\` (both windows), `C:\Projektit\runs-od-wt-1554\`.

## The bar (from the issue's checklist)

- Per camera per fixture: bow-vs-radius + conic-residual measurements, fitted k1 (and f) with confidence, residual before/after — committed as a census where the constants live.
- The centred-board trap handled by name; uninformative cameras say so.
- Synthetic control: footage-shaped data at known k1 recovers it within stated tolerance; the mutation (undistorted synthetic) recovers ~0 — predictions stated first.
- A one-paragraph verdict posted as a comment to #1513 (closed — comment anyway, it is the decision record) and to #1560, each starting with the line `> *This was generated by AI during triage.*`
- Pure logic inline (#1338 shape), tester registered: **rows DIRECTLY UNDER the `1551-admission` row.** Never append at the tail.
- MSVC: `gh workflow run "Build and release" --repo Apaiko-Oy/OpenDartboard --ref issue-1560-k1-census`.

## Docker is a shared single resource

**Do not start any container until the supervisor pings "Docker is yours".** #1552's batch holds it now. The calibration transcripts on disk may already carry the wire endpoints you need — mine them first; the fit itself is pure math on extracted points and runs host-side.

## Boundaries

- A second agent is live on **#1552** in `C:\Projektit\od-wt-1552`, editing `dart_processing.*` and the census scripts. **Do not edit `dart_processing.*`, `i1484_confidence_census.py`, or `i1510p2_census.py`.** Your territory: new tester files + a census record beside the constants; if your f disagrees with `perspective_processing.cpp`'s 120°, REPORT the number, don't change the constant (that flip is a separate decision).
- Its branch may merge before yours: merge origin/main before your verification batch if main moved.
- The board is unlit: never open live cameras. Never modify `mocks/`. Never judge against `mocks/cam_*.mp4` (#1478). Never push main, never `git stash`, never kill by pattern, no credentials.
- Commits: `#1560: <sentence>`; end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

## Report

Branch pushed, per-camera k1/f table with confidence and board-position sensitivity, before/after residuals, the systematic-or-random verdict, synthetic control + mutation verbatim, MSVC run id, the two issue comments posted, anything out of scope (report, don't file).
